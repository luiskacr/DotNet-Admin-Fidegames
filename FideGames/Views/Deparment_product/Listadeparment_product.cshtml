@model IEnumerable<FideGames.Models.deparment_product>

@{
    ViewBag.Title = "Listadeparment_product";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@if (ViewBag.exito == "" | ViewBag.exito == null)
{

}
else
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <button type="button" class="close" data-dismiss="alert">x</button>
        <span class="aria-hidden">@Html.ViewBag.exito</span>
    </div>

}
@if (ViewBag.error == "" | ViewBag.error == null)
{

}
else
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <button type="button" class="close" data-dismiss="alert">x</button>
        <span class="aria-hidden">@ViewBag.error</span>
    </div>

}

<div class="card-body card shadow mb-4">
    <style>
        .table-title {
            padding-bottom: 15px;
            background: #040FD9;
            color: #fff;
            padding: 16px 30px;
            min-width: 100%;
            margin: -20px -25px 10px;
            border-radius: 3px 3px 0 0;
        }

            .table-title h2 {
                margin: 5px 0 0;
                font-size: 24px;
            }

            .table-title .btn-group {
                float: right;
            }

            .table-title .btn {
                color: #fff;
                float: right;
                font-size: 13px;
                border: none;
                min-width: 50px;
                border-radius: 2px;
                border: none;
                outline: none !important;
                margin-left: 10px;
            }
    </style>
    <div class="table-title">
        <div class="row">
            <div class="col-sm-6">
                <h2>Departamentos de productos</h2>
            </div>
            <div class="col-sm-6">
                @Html.ActionLink("Agregar nuevo departamento de productos", "Creardeparment_product", null, new { @class = "btn btn-primary btn-lg" })
            </div>
        </div>

    </div>
    <div class="table-responsive">
        <table id="TableEmployee" class="table table-avance table-striped ">
            <thead class="table-dark">
                <tr>
                    <th class="">
                        Nombre de departamento de productos
                    </th>
                    <th class="d-flex justify-content-center ">
                        Acciones
                    </th>
                </tr>
            </thead>
            <tbody class=" ">
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.deparment_product_name)
                        </td>
                        <td class="d-flex justify-content-center ">
                            @Html.ActionLink("Editar", "Editardeparment_product", new { id = item.deparment_product_id }, new { @class = "btn btn-primary mr-2" })
                            @Html.ActionLink("Detalles", "Detallardeparment_product", new { id = item.deparment_product_id }, new { @class = "btn btn-primary mr-2" })
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
